/**
 * Module: audit
 * Ghi và tra cứu audit log thao tác nhạy cảm
 *
 * Domain layer — entity, value object, enum trạng thái, business rule thuần (không phụ thuộc framework/DB).
 *
 * Phase 1 (scaffold): chưa implement nghiệp vụ. Các phase sau bổ sung tại đây.
 * Tham khảo docs/data-model.md và docs/architecture.md.
 */
export {};
